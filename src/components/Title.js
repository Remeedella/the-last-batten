import React from "react";

const Title = ({ title }) => (
    
        <div className="position-fixed start-50"><h1 className="mx-auto display-2 fw-bold text-center align-middle">{title}</h1></div>
    
);
  export default Title